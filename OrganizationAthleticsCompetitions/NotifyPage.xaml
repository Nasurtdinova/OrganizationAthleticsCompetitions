<Page x:Class="OrganizationAthleticsCompetitions.NotifyPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:OrganizationAthleticsCompetitions"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="NotifyPage">

    <Grid>
        <StackPanel>
        <TextBlock Text="Уведомления" Margin="10"></TextBlock>
        <ListView x:Name="lvNotifiies">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                        <TextBlock>
                            <Run Text="Ваша заявка о спонсировании команды"></Run>
                            <Run Text="{Binding Team.Name}"></Run>
                            <Run Text="{Binding Status.Name}" TextDecorations="Underline"></Run>
                        </TextBlock>
                            <Button x:Name="btnCancel" Click="btnCancel_Click" Visibility="{Binding VisibilityCansel}">Отменить</Button>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
        </ListView>
        </StackPanel>
    </Grid>
</Page>
